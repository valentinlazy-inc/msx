{
  "name": "ym-msx-server",
  "version": "0.3.0",
  "private": true,
  "description": "Unified static server and provider resolvers for YummyAnime MSX",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "./scripts/build-dist.sh",
    "build-static": "./scripts/build-static.sh",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "playwright": "^1.52.0",
    "wrangler": "^4.11.1"
  }
}
